<div class="">
  <%= link_to("Back", {:action => 'index'}) %>

  <%= form_for(:work_time, :url => {:action => "drop", :id => @work_time.id}) do |field|%>
    <p>Do you want to delete this?</p>

    <%= submit_tag("Delete") %>
  <% end %>
</div>
