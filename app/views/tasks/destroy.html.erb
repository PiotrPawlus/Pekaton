<div class="">
  <%= link_to("Back", {:action => 'index'}) %>

  <%= form_for(:task, :url => {:action => "drop", :id => @task.id}) do |field|%>
    <p>Do you want to delete this?</p>

    <%= submit_tag("Delete") %>
  <% end %>
</div>
