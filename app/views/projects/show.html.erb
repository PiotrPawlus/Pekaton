<header id="index-header">
    <div class="container-fluid">
        <div class="row">
            <a href="/"><%= image_tag("logo-black.png", alt: "Logo PetaTime") %></a>
            <!-- <img src="/images/logo-black.png" alt="LOGO" class="pull-left"> -->
            <ul class="pull-right">
                <li class="pull-left"><a href="/works_time">Projects show</li>
                <li class="pull-left">
                    <% if user_signed_in? %>
                        <%= link_to "Wyloguj", destroy_user_session_path, :method => :delete %>
                    <% else %>
                        <%= link_to('Login', new_user_session_path)  %>
                    <% end %>
                </li>
                <li class="pull-left"><i id="open-sidebar" class="fa fa-bars" aria-hidden="true"></i></li>
            </ul>
            <div class="clearfix"></div>
        </div>
    </div>
</header>

<!-- <section class="edit-delete">
</section> -->

<section id="leader_board">
  <div class="container">
    <div class="row">
      <div class="col-xs-12" style="text-align: center;">
        <h2 class="text-center">Projects</h2>
        <%= link_to({:action => 'index'}, :class=>"btn btn-default margin-bottom-25") do%>
          <i class="fa fa-arrow-left" aria-hidden="true"></i> Back
        <% end %>
      </div>
    </div>
    <div class="row">
      <div class="col-xs-12">

        <table class="table table-striped table-hover" id="example">
          <tr>
            <th>ID Task</th>
            <th>Task Name</th>
            <th>Is done</th>
            <th>Task data of create</th>
            <th>Task data of last update</th>
          </tr>
          <% @tasks.each do |task| %>
            <% if task.project_id === @project.id %>
            <tr>
              <th><%= task.id %></th>
              <th><%= task.title %></th>
              <th><%= task.is_done %></th>
              <th><%= task.created_at%></th>
              <th><%= task.updated_at %></th>
            </tr>
            <% end %>
          <% end %>
        </table>

      </div>
    </div>
  </div>
</section>


  <footer style="bottom: 0; left: 0; right: 0; position: fixed;">
    <div class="container">
        <div class="row">
            <div class="col-xs-12 text-center footer-content">
                <%= image_tag("logo-white.png", alt: "Logo PetaTime") %>
                <p>© Copyright 2016 PetaTime - All Rights Reserved. Powered by Ruby on Rails.</p>
                <p>Made by Kuba & Piotrek</p>
            </div>
        </div>
    </div>
</footer>