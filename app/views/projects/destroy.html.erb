<div class="">
  <%= link_to("Back", {:action => 'index'}) %>

  <%= form_for(:project, :url => {:action => "drop", :id => @project.id}) do |field|%>
    <p>Do you want to delete this?</p>
    <%= submit_tag("Delete") %>
  <% end %>
</div>
